spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authdb
    username: postgres
    password: 15Bhzxc778@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

springdoc:
  api-docs.path: /custom-docs
  swagger-ui.path: /custom-ui.html

jwt:
  secret: ${JWT_SECRET:TaK+HaV^uvCHEFsEVfypW#7g9^k*Z8$V} //256bits long
  access-token-expiration: 3600000 # 60 minutes in milliseconds
  refresh-token-expiration: 604800000 # 7 days in milliseconds
