# Template only — copy to 20-secret.yaml and fill in. NEVER commit real values.
apiVersion: v1
kind: Secret
metadata:
  name: loqal-secrets
  namespace: loqal
type: Opaque
stringData:
  POSTGRES_USER: "replace"
  POSTGRES_PASSWORD: "replace"
  JWT_RSA_PRIVATE_KEY: "base64-pkcs8-private-key"
  JWT_RSA_PUBLIC_KEY: "base64-spki-public-key"
  GOOGLE_CLIENT_ID: "replace"
  GOOGLE_CLIENT_SECRET: "replace"
  RAZORPAY_KEY_ID: "replace"
  RAZORPAY_KEY_SECRET: "replace"
  RAZORPAY_WEBHOOK_SECRET: "replace"
